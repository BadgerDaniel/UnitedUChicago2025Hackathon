{
  "mcpServers": {
    "playwright-trends": {
      "command": "mcp-trends-server",
      "description": "Custom Playwright server with Google Trends keyword and location-based scraping tools."
    },
    "playwright-generic": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest"
      ],
      "description": "The standard, public Playwright MCP server with only the original browser automation tools."
    }
  }
} 